<h1>Dashboard</h1>

<% if user_signed_in? %>
Signed in as <%= current_user.name %>. Not you?
<%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
<% end %>

<br />

<% Service.all.each do |service| %>
  <%= fa_icon(service.icon, text: service.name) %><br />
<% end %>
